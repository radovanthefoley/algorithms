public class CountingBits {
    public int countingBits(int n) {
        return 0;
    }

    public static void main(String... args) {
        CountingBits cb = new CountingBits();
        System.out.println(cb.countingBits(5));
    }
}
